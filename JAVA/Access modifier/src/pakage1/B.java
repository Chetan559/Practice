package pakage1;
import pakage2.*;

public class B {
    private String privateMessage = "This is private message";
//     the variable declared using private access modifier can be accessed only from the class that contains it
    System.out.println(privateMessage);
}
